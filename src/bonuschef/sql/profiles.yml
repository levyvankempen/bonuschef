dbt_bonuschef:
  outputs:

    default:
      type: "postgres"
      host: "localhost"
      user: "postgres"
      password: "postgres"
      port: 5455
      dbname: "postgres"
      schema: "public"
      threads: 1

  target: "{{ env_var('ENVIRONMENT', 'default')}}"