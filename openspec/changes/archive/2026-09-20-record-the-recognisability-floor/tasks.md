# Tasks

The behaviour shipped in #70 and #72 and is live in v1.11.8. These tasks
record it; there is no implementation left to do.

- [x] 1. Withhold the cohort when its best candidate is unrecognisable (#70)
- [x] 2. Recognise diminutives, trailing qualifiers and compound heads, so the
      floor does not withhold correct products (#70)
- [x] 3. Apply the naming test to resolutions already recorded (#72)
- [x] 4. Withdraw an unrecognisable product unconditionally and flag the
      concept, rather than keeping it because nothing replaces it (#72)
- [x] 5. Record the above in `recipe-catalogue`, correcting the two statements
      it made that are no longer true
