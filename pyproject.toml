[project]
name = "recipechecker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[workspace]>=1.16.0",
    "dotenv>=0.9.9",
    "duckdb>=1.3.2",
    "marimo>=0.15.3",
    "nox>=2025.5.1",
    "pendulum>=3.1.0",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
]
