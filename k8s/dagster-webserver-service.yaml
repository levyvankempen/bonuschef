apiVersion: v1
kind: Service
metadata:
  name: dagster-webserver
  namespace: bonuschef
spec:
  type: NodePort
  selector:
    app: dagster-webserver
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
